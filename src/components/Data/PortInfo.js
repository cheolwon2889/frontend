import styles from './DockSimulation.module.css';
import React, { useState } from 'react';


function PortInfo({ portId }) {
    return (
        <div>
            Port Info about {portId}
        </div>
    );
}

export default PortInfo;
